/**
 * Fix #145 — Voir la version JavaScript : tests/admin/site-config-merge.test.js
 *
 * Le test est en JS pur pour fonctionner partout (Mac, Linux, sans esbuild).
 * Exécution : node tests/admin/site-config-merge.test.js
 *
 * Ce fichier .ts est un stub vide laissé pour éviter de casser des outils qui
 * auraient déjà référencé ce chemin (CI, scripts internes).
 */
export {};
